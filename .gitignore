# 1. Môi trường ảo (Đã cập nhật theo tên mới của Ngài)
ratvenv/
exnessvenv/
venv/
env/

# 2. Xử lý triệt để Cache (Áp dụng cho core/ và mọi thư mục con)
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.pyd
.pytest_cache/
.coverage
htmlcov/

# 3. Dữ liệu Bot & Log (Bỏ qua toàn bộ thư mục data)
data/
!data/templates/        # [NGOẠI LỆ]: Cho phép lưu thư mục Templates chiến thuật
!data/templates/*.json

*.log

# 3.1 Xử lý JSON
*.json
!data/templates/*.json  # [NGOẠI LỆ]: Giữ lại các file JSON chiến thuật để push lên Git

# 4. Cấu hình nhạy cảm (Cảnh báo DevSecOps)
# Nếu config.py chứa API Key hoặc Password của Ngài, nên bỏ qua nó 
# và dùng một file config.py.example để push lên.
# config.py 

# 5. IDE & Hệ thống
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp